{
  "name": "nested_form_builder",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "builder:install": "npm --prefix builder install",
    "builder:dev": "npm --prefix builder run dev",
    "builder:build": "npm --prefix builder run build",
    "builder:preview": "npm --prefix builder run preview",
    "clasp:login": "npx --yes @google/clasp login",
    "clasp:push": "npx --yes @google/clasp push",
    "clasp:pull": "npx --yes @google/clasp pull"
  },
  "devDependencies": {
    "@google/clasp": "^3.0.6-alpha",
    "@playwright/test": "^1.56.1",
    "playwright": "^1.56.1"
  }
}
